import React from 'react'

function Rating( {rating, style, onClick} ) {

    console.log(rating);

    return (
        <>
            {
                [...Array(5)].map(()=> {
                    
                })
            }
        </>
    )
}

export default Rating
